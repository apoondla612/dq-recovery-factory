Place the sealed Test-Mappings-Export.zip here.

The factory refuses a bare path. `dq recover --export packages/Test-Mappings-Export.zip` registers the archive SHA and each member SHA before any parse.

The zip is source evidence, not a generated artifact. It is gitignored.
